In this folder you will find the Map images of your different routes.

The images are stored in png format, and the file names follow the pattern
'<route_id>_<route_leg>.png>' where route_id comes from the current timestamp